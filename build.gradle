buildscript {
    repositories {
        maven { url nexusUrl }
    }

    dependencies {
        classpath "gradle.plugin.org.liquibase:liquibase-gradle-plugin:1.2.4"
    }
}

//mixed style does not work
plugins {
    id 'java'
}

//so it works
//apply plugin: 'java'
apply plugin: 'org.liquibase.gradle'

